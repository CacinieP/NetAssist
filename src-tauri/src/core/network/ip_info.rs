// IPInfoDetector and detect() removed — dead code (TODO stub always returning default).
// Actual IP detection is in commands/ip_info.rs using standalone functions.
// IP type classification is the private `classify_ip_type` in commands/ip_info.rs.
